import { type BlendMode, type PropDriver, type TransformConfig } from '../../core';
import { type ComponentProps } from '../../core/shaders/Exposure';
/**
 * Define component props including blend mode, opacity, visibility, masking, and transformation
 */
interface ExtendedComponentProps extends Partial<ComponentProps> {
    exposure?: ComponentProps['exposure'] | PropDriver;
    blendMode?: BlendMode;
    opacity?: number;
    visible?: boolean;
    id?: string;
    maskSource?: string;
    maskType?: string;
    renderOrder?: number;
    transform?: Partial<TransformConfig>;
    children?: import('svelte').Snippet;
}
declare const Exposure: import("svelte").Component<ExtendedComponentProps, {}, "">;
type Exposure = ReturnType<typeof Exposure>;
export default Exposure;
